# Contract changelog

`packages/shared` is the frozen contract. Every enum, DTO and pure function in
it is imported by **both** the API and the Angular app, and nobody redefines any
of it locally.

## The rule

**Frozen after Phase 3.** Adding an optional field is cheap; changing or removing
one breaks another agent's compile at the worst possible moment.

Before you change anything in `packages/shared`:

1. **Prefer adding an optional field** over changing an existing one.
2. Say so in the team channel _before_ you edit, naming the type.
3. Add a row to the table below.
4. Run `npm run verify` — it typechecks all three packages together, so it will
   catch every downstream break in one pass.
5. Merge the shared change **on its own**, before the work that needs it.

If you cannot avoid a breaking change, it is the integrator's call, and it
happens at a checkpoint (T+8 / T+14 / T+19), never between them.

## What counts as breaking

| Change                                | Breaking?                                         |
| ------------------------------------- | ------------------------------------------------- |
| Adding an optional field to a DTO     | No                                                |
| Adding a new enum member              | No — unless something switches exhaustively on it |
| Adding a new exported function        | No                                                |
| Renaming or removing a field          | **Yes**                                           |
| Making an optional field required     | **Yes**                                           |
| Changing a field's type               | **Yes**                                           |
| Changing what a pure function returns | **Yes**                                           |
| Changing a risk threshold _default_   | No — but say so, it moves the demo numbers        |

## Log

| #   | Date    | Type     | What changed                                                                                                                                                                                                                                                               | Why                                                                                                                                                                                                                                                                                                                                | Who       | Downstream                                                              |
| --- | ------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ----------------------------------------------------------------------- |
| 1   | T+0     | Baseline | Initial contract: enums, entity DTOs, request/response DTOs, and the nine pure functions (`computeLinePricing`, `computeQuoteTotals`, `computeMargin`, `calculateBlendedRisk`, `resolveApprovalChain`, `prorate`, `nextBillingDates`, `rankUpsells`, `planWarehouseSplit`) | Phase 1                                                                                                                                                                                                                                                                                                                            | Architect | —                                                                       |
| 2   | T+10    | Additive | `AuditEntity` gains `PRICELIST`, `WAREHOUSE`, `SUBSCRIPTION_PLAN`                                                                                                                                                                                                          | The catalogue writes (A-19..A-22) audit-log against their own entity rather than borrowing `CONFIG`. Nothing switches exhaustively on `AuditEntity`, so this is non-breaking.                                                                                                                                                      | A         | none — additive enum members                                            |
| 3   | T+10    | Additive | New request DTOs: `UpdatePriceListRequest`, `UpsertWarehouseRequest`, `UpsertSubscriptionPlanRequest`                                                                                                                                                                      | `PUT /pricelists/:id`, `POST`/`PUT /warehouses` and `POST /subscription-plans` needed typed bodies; `UpsertProductRequest` already existed.                                                                                                                                                                                        | A         | none — new exported types                                               |
| 4   | T+21    | Additive | New request DTO: `UpsertCustomerRequest`; `AuditEntity` gains `CUSTOMER`                                                                                                                                                                                                   | `POST /customers` and `PATCH /customers/:id` — the last endpoints left in the `customers` module's `todo` list — needed a typed body and their own audit entity, matching the `UpsertWarehouseRequest` / `WAREHOUSE` pattern.                                                                                                      | A         | none — new exported type and additive enum member                       |
| 5   | Phase E | Additive | `ReportingDashboardDto` gains `avgApprovalSlaHours` and `avgApprovalWithinSla`; new constant `APPROVAL_SLA_HOURS` (24) in `constants.ts`                                                                                                                                   | P2 #37 — the reporting dashboard highlights the average approval time against a service-level target, and the approval queue flags a pending item that has sat past it. Both new fields are always present on the response (not optional), but nothing consumed the DTO without going through the store, so this is safe additive. | E         | reporting page reads the two fields; `approval-list` reads the constant |
| 6   | Phase E | Additive | New DTOs: `NotificationListDto`, `MarkAllReadResponse`                                                                                                                                                                                                                     | P2 #35 — the notification centre needs a list shape carrying `unreadCount` for the header badge, and a typed body for `POST /notifications/read-all`.                                                                                                                                                                              | E         | `NotificationStore`, `notification-bell`                                |
| 7   | Phase E | Additive | New DTOs: `ReissuePortalLinkRequest`, `ReissuePortalLinkResponse`                                                                                                                                                                                                          | P2 #36 — `POST /quotations/:id/portal-link` reissues a customer magic link (revoke every live token, mint one fresh).                                                                                                                                                                                                              | E         | quotation-detail + approval-detail "Reissue customer link"              |

| 8   | Phase E | Additive | New DTOs: `PortalQuotationSummaryDto`, `PortalQuotationListResponse`; `PortalResolveResponse` gains `siblingCount`                                                                                                                                                          | `GET /portal/quotations` — one customer has many quotations, so the portal opens on the company's list instead of pinning itself to whichever quotation was linked last. `siblingCount` lets the detail screen decide whether to show the switcher without a second call. It is always present on the response (not optional), and the only consumers go through `PortalStore`, so this is safe additive. | E         | `PortalStore`, `portal-quotations` / `portal-quotation` / `portal-messages` / `portal-profile` pages |

| 9   | Phase E | Additive | `KanbanBoardDto` columns gain `cardCount`; new constant `NO_MATCHES` in `constants.ts`                                                                                                                                                                                     | Search and pagination on every list view. A Kanban column shows at most `cardsPerColumn` cards, so it has to report the true number behind them rather than let `cards.length` pass for the count. `NO_MATCHES` is the copy shown when a *search* empties a list — a different fact from an empty collection, and previously indistinguishable on screen. `cardCount` is always present (not optional); the only consumer is `df-kanban-board`. | E         | `df-kanban-board`, `df-empty-state`, every list page |

| 10  | Phase E | Additive | New DTOs: `ProductWarehouseStockDto`, `ProductStockDto`, `ProductWarehouseStockInput`; `UpsertProductRequest` gains optional `warehouseStock`; new exports `STOCKED_CATEGORIES` and `isStockedCategory(category)` in `enums/index.ts` | Warehouse stock on the product screens. `GET /products/:id/stock` backs screen 17's Warehouse Stock card, and `POST /products` accepts an opening allocation so a hardware product is stocked where it is created. `isStockedCategory` is the single definition of "HARDWARE is the category that sits in a warehouse", shared by the API's validation, the seed assertion and both product screens, so the FE and BE cannot disagree about which products get the card. All additive: new types, one optional request field, and two new exported values. | E         | `product-detail` (Warehouse Stock card), `product-list` (New Product form), `products.routes`, `scripts/reset-env` |

| 11  | Phase E | Additive | New constants `FINANCE_QUOTATION_STAGES` (`constants.ts`) and `QUOTATION_WRITE_ROLES` (`enums/index.ts`) | Finance's list screens are scoped to Finance's work: quotations from APPROVED onward, and only the approvals that reached the Finance step. The scope is enforced server-side in `utils/roleScope.ts`; these two constants are shared so the UI describes exactly the rule it is being served — the subtitle on each list, and hiding **+ New Quotation** for a role the API would 403. `QUOTATION_WRITE_ROLES` also replaces the API's local `WRITE_ROLES` array, so there is one definition instead of two. Both additive: new exported values, no existing shape touched. | E         | `quotation-list`, `approval-list`, `quotations.routes`, `approvals.routes`, `utils/roleScope` |

| 12  | Phase E | Additive (one removal) | New DTOs `CreateUserRequest`, `UpdateUserRequest`, `UserRowDto`, `UserListDto`; new `EMPTY_STATES.users`. **Removed:** `SignupRequest` is no longer served — `POST /auth/signup` is gone | Screen 19, the Admin Users tab: accounts are issued by an Admin, never self-served, and a CUSTOMER account carries the company it is scoped to (D-034). `UserRowDto` adds the resolved `customerName` so the list shows who a portal login belongs to without a request per row. The signup DTO is left exported but unused by either side, so nothing breaks at compile time; the endpoint and the `/signup` screen are both deleted. | E         | `users.page` (new), `users.routes`, `auth.routes`, `login.page`, `admin-shell` |

| 13  | Phase E | Additive | `UserDto` gains `mustChangePassword` (always present); `CreateUserRequest` gains `mustChangePassword?`; `UpdateUserRequest` gains `email?`, `password?`, `mustChangePassword?`; new `ChangePasswordRequest` | The Admin can now edit an account, not just create and deactivate it, and a new account is asked to choose its own password at first sign-in (D-036). `mustChangePassword` is not optional — the sign-in flow branches on it immediately, and `toUserDto` normalises it to a boolean so a row written before the field existed reads as `false` rather than `undefined`. The only consumers go through `SessionStore` and the users screen. | E         | `session.store`, `login.page`, `change-password.page` (new), `users.page`, `auth.routes`, `users.routes` |

<!-- Append below. One row per change. -->
