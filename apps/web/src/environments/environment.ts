export const environment = {
  production: false,
  apiBase: 'http://localhost:3000/api/v1',
  /** Shows the one-click "Demo accounts" panel on the login screen. */
  showDemoLogins: true,
  /**
   * Serve every request from in-memory fixtures instead of the API.
   * Any frontend workstream can keep moving while its backend counterpart is
   * still building. Flip this on, reload, and every screen still renders.
   */
  useMocks: false,
  appName: 'DealFlow360',
};
